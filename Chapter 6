Lists 
Lists and tuples can contain multiple values, which makes writing programs that handle large amounts of data easier.
A list is a value that contains multiple values in an ordered sequence. We call values inside the list items
-1 and -2 inter values refer to the lasst and second to last indexes in a list

slice lists using [n:m]
len() function tells you the length of the list
you can update certain values in lists
concatentaion and replication

adding lists together
can be done using the + and * to multiply the lists just like you would to strings
del statement will delete values at an index in a list.

the "in" an "not in" operators return true or false 

 you can call the enumerate() function instead of using range(len(list)) - however it will return two values
index and the item.

Methods are called on values
each data type has its own methods

you can add values to a list using .append() or .insert()
names for methods are quite straight forwards so - .remove() removes values

.sort() orders strings in alphabetical order and numbers in numerical

A list value is a mutable data type: you can add, remove, or change its values. However, a string is immutable: it cannot be changed.

tuples are different to lists in that they are immutable and you can use parantheses instead of square brackets
