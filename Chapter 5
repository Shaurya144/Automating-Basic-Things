
Python raises an exception whenever it tries to execute invalid code.
#try and except statements so that your program could recover from exceptions you anticipated
raise exceptions with :
The raise keyword
A call to the Exception() function
A string with a helpful error message passed to the Exception() function

the logging module in python can be used to determine where in the code a mistake takes place as it updates a log for 
each part of code and if a log is skipped you know there is an error in the code
You can write log messages to declutter your screen 
IT IS BAD PRACTICE TO USE PRINT() in code instead as can take a long time to clear up.

there are different logging functions : debug, info, warning, error, critical 
